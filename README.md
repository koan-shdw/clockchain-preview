# Clockchain — preview build

Interactive redesign mockup for client review. Static site (HTML/CSS/JS).

- Main site: `index.html`, `about.html`, `newsroom.html`
- App + docs mock: `app/` (login → dashboard → services → docs)

Hosted on GitHub Pages. Live values use a simulated feed; production wiring notes are in `widget.js` and the app pages. Handoff build for D4D / Clockchain.
